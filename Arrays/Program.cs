using System;
namespace Arrays
{
    class Program
    {
        static void Main(string[] args)
        {
            // Basics.Run();
            // numbers[2] = 9;
            // System.Console.WriteLine(numbers[1]);
            // facebookLike.Run();
            // reverseName.Run();
            // SortFive.Run();
            RemoveDuplicates.Run();
        }
    }
}
